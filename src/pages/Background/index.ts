// import { ACTIONS } from '../modules/actions';
// chrome.runtime.onMessage.addListener(function (arg, sender, sendResponse) {
//   const { type, payload } = arg;
//   if (type === ACTIONS.SOMETHING) {
//     sendResponse('');
//   }
// });
